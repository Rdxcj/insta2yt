name: .

on:
  workflow_dispatch:
  
jobs:
  In2Yt:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4
    - name: Pull Docker image
      run: docker pull qsdcbv/insta2yt:v9
    - name: Run Docker file
      run: |
           docker run qsdcbv/insta2yt:v9 sh -c "wget https://raw.githubusercontent.com/Rdxcj/Test3/main/57.py && ls -tgh && pwd && ./geckodriver --version && wget https://raw.githubusercontent.com/Rdxcj/Test3/main/44.py && firefox --version && which firefox && python3 44.py"
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
          name: my-artifact # NOTE: same artifact name
          path: /shit
          compression-level: 9
